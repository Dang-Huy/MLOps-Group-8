apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: credit-score-artifacts-pvc
  namespace: credit-score
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard

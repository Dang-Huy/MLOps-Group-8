apiVersion: v1
kind: Service
metadata:
  name: credit-score-api
  namespace: credit-score
  labels:
    app: credit-score-api
spec:
  selector:
    app: credit-score-api
  ports:
    - name: http
      port: 80    
      targetPort: 8000
      nodePort: 30100  
  type: NodePort
